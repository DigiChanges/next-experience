import React from 'react';
import {Expiredlink} from "@/features/auth/expired-link/organisms/ExpiredLink";

export const ExpiredLinkTemplate: React.FC = () => {
    return (
        <Expiredlink />
    );
};
